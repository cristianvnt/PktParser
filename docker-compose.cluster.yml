services:
  cassandra-head:
    image: ${CASSANDRA_IMAGE}
    container_name: cass-head
    ports:
      - ${CASSANDRA_PORT}:9042
      - 7000:7000
    networks:
      - cassandra-net
    environment:
      - CASSANDRA_CLUSTER_NAME=${CASSANDRA_CLUSTER_NAME}
      - CASSANDRA_SEEDS=cassandra-head
      - CASSANDRA_LISTEN_ADDRESS=auto
      - CASSANDRA_BROADCAST_ADDRESS=cassandra-head
      - CASSANDRA_DC=${CASSANDRA_DC}
      - CASSANDRA_ENDPOINT_SNITCH=GossipingPropertyFileSnitch
      - MAX_HEAP_SIZE=${CASSANDRA_HEAP}
      - HEAP_NEWSIZE=${CASSANDRA_NEWGEN}
    mem_limit: ${CASSANDRA_MEM_LIMIT}
    volumes:
      - cassandra-node-head:/var/lib/cassandra
      - ./config/cassandra.yaml:/etc/cassandra/cassandra.yaml
    restart: on-failure
    healthcheck:
      test: ["CMD-SHELL", "cqlsh -e 'SELECT now() FROM system.local'"]
      interval: 30s
      start_period: 3m
      timeout: 10s
      retries: 5

  cassandra-0:
    image: ${CASSANDRA_IMAGE}
    container_name: cass-0
    networks:
      - cassandra-net
    environment:
      - CASSANDRA_CLUSTER_NAME=${CASSANDRA_CLUSTER_NAME}
      - CASSANDRA_SEEDS=cassandra-head
      - CASSANDRA_LISTEN_ADDRESS=auto
      - CASSANDRA_BROADCAST_ADDRESS=cassandra-head
      - CASSANDRA_DC=${CASSANDRA_DC}
      - CASSANDRA_ENDPOINT_SNITCH=GossipingPropertyFileSnitch
      - MAX_HEAP_SIZE=${CASSANDRA_HEAP}
      - HEAP_NEWSIZE=${CASSANDRA_NEWGEN}
    mem_limit: ${CASSANDRA_MEM_LIMIT}
    depends_on:
      cassandra-head:
        condition: service_healthy
    volumes:
      - cassandra-node-0:/var/lib/cassandra
      - ./config/cassandra.yaml:/etc/cassandra/cassandra.yaml
    restart: on-failure
    healthcheck:
      test: ["CMD-SHELL", "cqlsh -e 'SELECT now() FROM system.local'"]
      interval: 30s
      start_period: 3m
      timeout: 10s
      retries: 5

  cassandra-1:
    image: ${CASSANDRA_IMAGE}
    container_name: cass-1
    networks:
      - cassandra-net
    environment:
      - CASSANDRA_CLUSTER_NAME=${CASSANDRA_CLUSTER_NAME}
      - CASSANDRA_SEEDS=cassandra-head
      - CASSANDRA_LISTEN_ADDRESS=auto
      - CASSANDRA_BROADCAST_ADDRESS=cassandra-head
      - CASSANDRA_DC=${CASSANDRA_DC}
      - CASSANDRA_ENDPOINT_SNITCH=GossipingPropertyFileSnitch
      - MAX_HEAP_SIZE=${CASSANDRA_HEAP}
      - HEAP_NEWSIZE=${CASSANDRA_NEWGEN}
    mem_limit: ${CASSANDRA_MEM_LIMIT}
    depends_on:
      cassandra-0:
        condition: service_healthy
    volumes:
      - cassandra-node-1:/var/lib/cassandra
      - ./config/cassandra.yaml:/etc/cassandra/cassandra.yaml
    restart: on-failure
    healthcheck:
      test: ["CMD-SHELL", "cqlsh -e 'SELECT now() FROM system.local'"]
      interval: 30s
      start_period: 3m
      timeout: 10s
      retries: 5

  elasticsearch:
    image: ${ELASTICSEARCH_IMAGE}
    container_name: es-node
    environment:
      - discovery.type=single-node
      - xpack.security.enabled=false
      - ES_JAVA_OPTS=-Xms${ES_HEAP} -Xmx${ES_HEAP}
    mem_limit: ${ES_MEM_LIMIT}
    ports:
      - 9200:9200
    networks:
      - cassandra-net
    volumes:
      - es-data:/usr/share/elasticsearch/data
    restart: on-failure
    healthcheck:
      test: ["CMD-SHELL", "curl -s http://localhost:9200/_cluster/health | grep -q '\"status\":\"green\\|yellow\"'"]
      interval: 30s
      start_period: 2m
      timeout: 10s
      retries: 5

networks:
  cassandra-net:
    driver: bridge

volumes:
  cassandra-node-head:
  cassandra-node-0:
  cassandra-node-1:
  es-data: